# DengShell 更新日志

## v0.01 R26 — 从 R20 直接升级的累计更新

发布日期：2026-09-14。程序构建号：`20260914026`。本次一次汇总 R21–R26 的全部主要变化，无需逐版安装。

### 命令管理

- 每条快捷命令可独立保存“末尾添加回车 CR”。默认不勾选，仅填入终端，方便继续补充参数；勾选后发送并执行。旧配置没有此选项的命令也默认不自动回车。
- 命令和命令分组支持拖动排序，顺序保存到本机配置；命令可拖到其他分组，空分组继续保留。分组支持右键重命名，右键菜单也提供前后移动。
- 编辑、删除及调用前编辑集成到右键菜单，移除胶囊旁常驻的编辑按钮。
- 新增命令编辑区，长命令和参数可反复修改、发送；草稿在本次运行中保留。终端输入条旁也可打开编辑区。
- 支持插入 1–5 号参数，例如 `docker logs [p#1 容器] --tail [p#2 行数]`；同号参数统一替换，发送前显示实际命令预览。
- 发送目标独立显示并绑定到原 SSH 会话，切换其他服务器标签不会更换编辑区的目标。未填写参数、无效控制字符或失效连接会阻止发送。
- 不加回车时移除末尾换行；终端未开启括号粘贴时，含换行或制表符的命令保留在编辑区，避免中间换行触发意外执行。

### 多服务器文本编辑

- 多个文件拥有各自独立的标签，显示服务器、文件名、“当前查看”和未保存标记；可同时打开不同服务器的文件。
- 支持将文件移到应用内可拖动、可调大小的独立编辑窗口，每个窗口有自己的标签栏；可以合并到其他编辑窗口。
- 每个文件单独保留内容、编码、光标、撤销与重做记录。读取与保存按文件独立处理，迟到的响应不会覆盖其他文件或新编辑。
- 保存始终使用原服务器、原路径及该文件的版本校验值，防止切换服务器后串写。远程文件发生变化时停止覆盖，草稿保留。
- 关闭未保存文件、退出应用时提示；断线后保留草稿供复制，保存中不允许关闭文件。收起窗口后可从文件工具栏恢复。
- 支持 `Ctrl+S` 保存、`Ctrl+Tab` 切换文件、`Ctrl+W` 关闭当前文件；复制、剪切和粘贴保留文本模型中的换行形式。

### FinalShell 迁移

- 设置新增“从 FinalShell 导入”。自动扫描可执行程序旁的 `finalshell_oot_pot` 文件夹及其子目录。
- 导入连接名称、地址、端口、账号和认证方式；密码自动解析并使用 DengShell 的加密配置保存，无需安装 Python 或外部解密插件。
- 密钥记录只有引用时显示“待配置私钥”，可直接补充关联，不会误当作密码登录或发起无效连接。
- 重复导入跳过已有连接，保留用户修改过的名称、密码和密钥；逐项报告不支持或失败的记录。

### Shell 与监控兼容

- 修复默认 Shell 为 Fish 等环境时系统信息、CPU、内存、磁盘、进程以及常用应用检查结果不完整的问题。后台脚本明确通过 POSIX Shell 执行。
- Bash、Zsh、Fish 支持会话集成：空闲状态、命令历史、终端目录跟随和提示符配色；正常加载原有 Shell 配置，不永久修改远端配置文件。
- 特殊字符、中文和换行目录采用明确的编码传递；单个 `/proc` 文件读取失败或进程退出不再中断整批进程采样。
- BBR、清理和 SWAP 检查使用兼容调用方式；不会在执行结束后退出用户的交互 Shell。国际测速脚本保持原有内容。

### 界面与字体

- 加深浅色模式标题、正文、辅助文字、占位提示和彩色按钮文字，提高可读性，覆盖管理器、监控和设置等界面。
- 内置 Noto 黑体、Noto 宋体、更纱 UI、霞鹜文楷屏幕版、Maple Mono CN 五套界面字体，包含简体中文、繁体中文和英文，随包保留 OFL 1.1 版权与许可。
- 内置界面字体可即时切换；支持导入 TTF、OTF、WOFF、WOFF2，导入后必须完整退出并重新启动才能使用。
- 浅色和深色界面文字颜色分别设置、保存及恢复默认；与终端字体、终端颜色和背景分开管理。

### 更新与安装包

- 更新清单强制验证内置发行公钥的 Ed25519 签名，结合 HTTPS、有效期、大小、SHA-256 和构建号防回退检查；下载与安装前再次检查。
- Windows 提供绿色 ZIP 和可在控制面板卸载的当前用户安装版；卸载保留个人配置。
- Linux 提供 x86-64 DEB、RPM、通用 tar.gz，新增原生 pacman `.pkg.tar.zst` 包，适用于满足依赖的 Arch Linux 桌面。
- 统一提供 R26 源码、更新日志和 SHA-256 校验清单；程序包排除用户配置、SSH 私钥、导入样本和发布签名私钥。

### 升级说明

退出旧程序，安装 R26 或解压替换程序，保留完整 `data/` 或 Linux 用户配置目录。R20 可以直接升级到 R26；之后重新连接服务器，让新的 Shell 集成生效。

Windows / Debian 的内置更新需要退出并重启，会中断当前 SSH 会话；RPM、Arch 与通用版使用对应的新安装包升级。Arch 可运行 `sudo pacman -U ./DengShell-linux-x64.pkg.tar.zst`。GitHub Release 不会自动部署官网的在线更新文件。

R20 客户端本身不验证更新签名，通过可信来源取得本版后才具备新的客户端签名校验。Windows SmartScreen / Authenticode 与更新清单签名不同。

本次验证及其边界见 [功能审查报告](build/FUNCTIONAL-AUDIT-r26.md)。新包体积增加主要来自五套完整中文界面字体，没有通过删除简繁中文字形来压缩体积。
