# DengShell v0.01 R50

内部构建号：20260919052。公开版本 v0.01，修订号 R50。

提供 Windows x64 安装版、绿色 ZIP，Linux x64 DEB、RPM、pacman、通用包以及源码 ZIP。macOS 暂停发布。

本次处理锁屏退出、标签宽度、快捷命令参数界面、已完成传输清理、服务器管理入口与 SFTP 显隐，以及临时 SSH 连接和连接后保存。FinalShell 导入以用户提供的连接导出为准；缺失代理的连接允许导入，并要求配置代理或确认直连后再连接。没有快捷命令导出，因此不增加无样本的快捷命令导入。

更新清单沿用既有签名密钥和地址，通过递增内部构建号让已安装的 R40 客户端识别 R50。保留既有更新日志和更新提示正文。

验证方法及边界见 [R50 功能复核](FUNCTIONAL-AUDIT-r50.md)。
